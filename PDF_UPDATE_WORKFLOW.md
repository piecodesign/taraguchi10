# 中国版PDF 更新ワークフロー

最終更新: 2026-08-02

この文書は、中国版の五珠資料が更新されたときに、PDFとサイトの差分を抽出し、人的判断を挟んで安全に反映するための作業履歴兼手順書です。

## 現在の基準点

- 対象サイト: `taraguchi10`
- 今回のPDF: `漱星阁开局指南2026.4.3.pdf`
- PDF版: 2026.4.3
- 直接打ち更新完了: `8baa68e`（D8-D13）、それ以前は `01d3660`〜`ff86ce3`
- 白4候補の大盤連動: `2afcc40`
- 間接打ち更新完了: `a074191`
- 直接打ち完了時バックアップタグ: `backup-direct-pdf-complete-20260801`

次回は、作業開始前に必ず最新コミットへ新しいバックアップタグを付ける。

## 掲載判断の基本方針

優先して確認・更新する情報:

1. 現サイトの五珠候補との差分
2. `A` だけだった候補に付いた `A1`、`A2` などの順位
3. 数字候補から `A` へ変わったもの（必勝結論の更新。最優先）
4. サイトにない白4図面（実戦性を人が判断する）
5. 必要に応じて「黒勝」「偏黒」「白近勝」などの評価原文

原則として自動反映しない情報:

- C情報
- 中国独自の白4名称
- 図面右側の中国語評価文
- 左右対称図のプライム付き重複候補
- A11以上を含む図面（必ず人的判断を挟む）

評価原文は判断画面へ表示するが、採用指示がない限りサイトへ掲載しない。

## 更新作業の流れ

### 1. 作業前の保護

1. `git status` で既存変更を確認する。
2. 最新コミットを確認する。
3. `backup-pdf-before-YYYYMMDD` のようなタグを作り、GitHubへpushする。
4. `test.html` と `data/test/` は本番コミットへ含めない。

### 2. PDFから情報を抽出する

作業用ファイルは次の場所に保持している。

- 作業ディレクトリ: `/Users/pieco_book/Dropbox/Claude/RenjuMemo_codex/test-staging`
- 抽出スクリプト: `extract_d3_d7.py`
- 判断反映スクリプト: `apply_d3_d7_decisions.py`
- 判断画面ひな型: `test.html`

`extract_d3_d7.py` は直接打ち・間接打ちの各グループに対応している。新しいPDFではページ範囲、PDFパス、版番号を更新する。

抽出時の注意:

- PDFはInDesignのベクターデータを優先して読む。
- OCRだけで候補座標を確定しない。
- 白石の「4」と黒5候補の数字「4」が同じページにある場合がある。
- 白4候補が複数検出されたページはPNGへレンダリングし、白石の4を目視確認する。
- `a1213` のように隣接ラベルが連結された場合は、`A12` と数字候補 `13` に分離する。
- C候補とプライム付き左右対称候補は比較対象から除く。

### 3. 判断画面を作る

ローカル専用の `test.html` に次を表示する。

- 現サイトの9×9盤
- PDF抽出後の9×9盤
- 図面ID（例: `i4-5`）
- 差分行ID（例: `i4-5-3`）
- 現サイトとPDFの候補値
- 差分の種類
- PDF右側の評価原文（未反映）
- 採用・不採用・その他の選択欄
- 自由記述欄
- ページ上部と最下部の「判断結果をコピー」ボタン

判断結果はブラウザのlocalStorageだけに保存される。Codexへ伝えるにはコピーボタンの内容をチャットへ貼り付ける。

### 4. 人的判断を伝える

図面全体を採用する例:

```text
i1-1: 採用
```

図面全体を不採用にする例:

```text
i1-3: 不採用
```

一部だけ採用する例:

```text
i1-4: その他 — 現サイトを採用のまま、Aの数字だけ確定
```

差分行を指定する例:

```text
d13-4: その他 — d13-4-1とd13-4-3は残し、他はPDFを採用
```

「採用」は、その図面の五珠候補差分をすべてPDFへ合わせる指示とする。ただし、中国語評価文は自動採用しない。

### 5. 判断をJSONへ反映する

反映時のルール:

- 採用: PDF候補を追加・更新し、PDFから消えた非C候補を削除する。
- 不採用: 現サイトを変更しない。
- 部分採用: 指定された差分行だけ変更する。
- A順位のみ: 現サイトとPDFの両方がA系の候補だけ更新する。候補の追加・削除はしない。
- 新規白4採用: 五珠図面を追加し、大盤の `baseBranches` に未使用アルファベットも追加する。
- 既存五珠図面に対応する大盤文字がない場合も補完する。
- 中国語評価原文は自動追加しない。
- Aへ変更されて矛盾した既存の数字評価だけ削除する。
- 「n題必勝」は確定したA候補数と整合させる。`A?` は確定数に含めない。

### 6. 公開前に検証する

最低限、次を確認する。

1. 全JSONが正常に読み込める。
2. `git diff --check` が成功する。
3. 変更対象が意図した珠型JSONだけである。
4. すべての五珠図面の白4座標が大盤の `baseBranches` に存在する。
5. 判断画面と `data/test/` がステージされていない。
6. ローカルサイトで対象珠型が読み込め、盤面が表示される。
7. 既存評価に、A化済みの数字候補を評価する文章が残っていない。

### 7. コミット・公開・バックアップ

1. 対象JSONだけを `git add` する。
2. 直接打ち・間接打ちなど、範囲が分かるメッセージでコミットする。
3. `main` をGitHubへpushする。
4. GitHub Pagesの公開JSONをキャッシュ回避付きで確認する。
5. 一連の更新完了後、`backup-pdf-complete-YYYYMMDD` タグを作ってpushする。

## 今回発生した重要な注意点

- PDFのベクター抽出の方がOCRより正しい場面が多かった。
- 数字の4と白石の4の混同は自動処理だけでは防げない。
- PDF上の隣接文字が連結され、A順位と数字候補が一つの文字列になることがあった。
- 現サイトに五珠図面があっても、大盤側の白4文字が欠けていることがあった。
- 珠型ごとにJSONの整形形式が異なるため、内容以外の大量差分を避ける必要がある。
- Pages公開はpush後すぐではなく、20秒程度の遅延が生じることがある。
- ブラウザのプルダウン操作だけではCodexへ判断内容は伝わらない。必ずコピーボタンで結果を渡す。

## 次回Codexへ渡す依頼文

```text
中国版PDFが更新されました。
PDF_UPDATE_WORKFLOW.mdの手順と前回の判断基準を使い、まず本番へ反映せずに差分判断画面を作ってください。
C情報・中国独自の白4名称・評価原文は自動反映しません。
A11以上、新規白4、数字からAへの変更を分かるようにしてください。
白石の4と数字候補4が競合するページは必ず画像確認してください。
```

## 今回のコミット履歴

- `01d3660`: D1 寒星のPDF差分反映
- `04be62f`: D2 渓月のPDF差分反映とD1評価整合
- `ff86ce3`: D3-D7のPDF差分反映
- `8baa68e`: D8-D13のPDF差分反映
- `2afcc40`: 白4候補を大盤へ追加
- `a074191`: I1-I13のPDF差分反映
